import './Loader.css'

const Loader = () => {

    return(
        <div style={{height: '100%'}}>
            <div style={{margin: 'auto'}} className='spinner'/>
        </div>
    )

}

export default Loader